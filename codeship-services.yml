fez:
  build:
    image: codeship_fez
    dockerfile_path: Dockerfile

fezstaging:
  build:
    image: codeship_fezstaging
    dockerfile_path: Dockerfile.staging

awsdeployment:
  image: uqlibrary/docker-base:5
  encrypted_env_file: "deployment.env.encrypted"

awsdeploymentstaging:
  image: uqlibrary/docker-base:5
  encrypted_env_file: "deployment.env.encrypted"
