# disabling whilst working on deployment
#- service: fpmtestrunner
#  command: .codeship/jet.sh
- service: fez
  type: push
  tag: staging # should only push on staging branch
  image_name: uql-registry.library.uq.edu.au/staging/fez
  image_tag: "{{.CommitID}}"
  registry: uql-registry.library.uq.edu.au
  encrypted_dockercfg_path: dockercfg.encrypted
- service: awsdeployment
  tag: staging # should only deploy on staging branch
  command: scripts/backend-deployment-staging.sh
